let age = 20;
let message = age >= 18 ? "you can drive" : "you cannot drive";

console.log(message);

let speed = 190;
let warning = speed >= 120 ? "Too Fast" : speed >= 80 ? "Fast" : "OK";
console.log(warning);
